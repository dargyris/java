package org.example;

public class Exercises {
    // Lists: insertSorted(): Insert integers in order in a list
    // should be implemented as a method in the list api.
    // HEAD -> 1 -> 2 -> 3 -> ...

    // Stack: isPalindrome(): Determine whether a string is a palindrome
    // eg. Racecar, I did, did I etc.

    // Queue: isPalindrome(): Do the same using a Queue

    // Sets: Abstract data type. Applies to any data structures.
    // Cannot contain duplicates.
    // HashSet (backed by hashTable)
    // LinkedHashSet (HashTable with DoublyLinkedList)
    // TreeSet (based on a TreeMap)
    // api is the same.

    // Didn't cover Graphs!
}
